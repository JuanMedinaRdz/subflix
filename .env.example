# Supabase (Phase B) — optional.
# If absent, the app falls back to localStorage-only demo mode.
# Get these from Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
